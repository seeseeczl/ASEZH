## 1. Patcher

- [x] 1.1 Add whitespace-flexible Find/Marker matching and style-preserving replace
- [x] 1.2 Insert `ZWriteModeLabels` from `ZWriteModeValues` and retarget Popup; applied only when both exist
- [x] 1.3 Match language toggle after source-code Draw + `GUI.color = bufferedColor` with flexible whitespace

## 2. Docs and version

- [x] 2.1 Document the installer whitespace and ZWrite array behavior
- [x] 2.2 Bump `package.json` to `0.0.3` and changelog
