# REL-ASEZH-0011 — 原生节点显示汉化

版本：0.0.11；计划标签：v0.0.11（本次为源码交付，未创建发布标签）。

## 范围

- 原生类型白名单和分类双重判断；排除 Flyme 分组、自定义类型及用户 Shader Function。
- 只翻译标题及非编辑态接口显示副本，保留重命名内容、自定义表达式参数、数学符号和 RGBA / UV 等通用标记。
- 语言切换更新文字测量，不修改名称字段、节点位置、连接、变量或算法。
- 已有安装回执的升级先移除旧补丁再接入，避免用已汉化源码覆盖最初备份；重复接入当前版本不写文件。

## 实测证据

环境：macOS，Tuanjie 2022.3.62t13，ASE 1.9.6.2。

| 检查 | 结果 |
| --- | --- |
| 运行时原生类型识别 | pass，284 个原生具体类型；15 个 Flyme 类型及 FunctionNode 排除 |
| 画布抽查 | pass，乘法、幂、底数、指数及主节点接口显示中文 |
| 标准 Shader 中英文完整文件 | pass，2265 字节完全相同 |
| URP Lit 模板中英文完整文件 | pass，120535 字节完全相同 |
| 测试图序列化及修改状态 | pass，中英文相同 |
| 实际安装撤回 | pass，15 个文件逐一匹配原始回执 SHA-256，撤回后无编译错误 |
| 实际重新接入 | pass，15 个文件写入成功 |
| 合成安装/撤回回归 | pass，六阶段及全部受管文件原始字节恢复 |
| Editor 单元测试 | pass，15/15，含 Flyme 排除及旧版回执保护 |
| 最终运行时扫描 | pass，32/32 已接入，284 个原生类型的默认标题均有翻译 |

标准 Shader SHA-256：`1296C39DA4F64BAE52DC3882334C64B489C088D87626C6C4400199260C6AFBC8`。

URP Shader SHA-256：`CA30BBEEC63A78B61F5177E761C5C2DBB6A0FAA0D914D6B291AD15D6F81130C8`。

外部证据：`/Users/long/Documents/ChatGPT/ASEZH/verification-native-0.0.11/`；画布截图和临时 Shader 位于实际项目 `Library/ASEZH/`。专有 ASE 源码和生成 Shader 不进入本仓库。

## 边界

Tuanjie 2022.3.61t9 + ASE 1.9.81 的历史正式发布门禁：not-run，本机不可用，不以当前环境替代。Windows、全部节点逐个可视化及所有管线渲染组合未验证。StickyNote 运行时枚举需要 OnGUI 上下文，未计作可视化通过。原用户 Shader 已存在未保存修改，本次未保存或覆盖该图。
